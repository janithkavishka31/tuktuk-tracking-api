# Replace with your Neon or Postgres connection string. Do NOT commit real credentials.
PORT=3000
DATABASE_URL="postgresql://<USER>:<PASSWORD>@<HOST>/<DB>?sslmode=require"
JWT_SECRET="replace_with_secure_random_value"
# Optional
# CORS_ORIGIN=*
# RATE_LIMIT_MAX=300
# AUTH_RATE_LIMIT_MAX=30
